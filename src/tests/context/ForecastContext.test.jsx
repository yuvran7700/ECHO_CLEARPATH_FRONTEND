// src/tests/context/ForecastContext.test.jsx
import { describe, it } from "vitest"

describe("ForecastContext", () => {
    it.todo("provides forecast data to children")
    it.todo("throws when used outside provider")
})

describe("ForecastProvider", () => {
    it.todo("fetches forecast data on mount")
    it.todo("updates context when location changes")
})